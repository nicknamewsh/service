package com.pt.ssm.service;

import com.pt.ssm.po.Material;
import com.pt.ssm.util.ModelBean;
import com.pt.ssm.util.ModelResults;

public interface MaterialService {

	ModelResults getMaterialList(long page, long size, String marterialName,
			String personName);

	ModelBean addMaterialInfo(Material material);

	ModelBean getMaterialInfoById(Integer id);

	ModelBean updateMaterialInfo(Material material);

	ModelBean delMaterialInfoByIds(long[] ids);

}
